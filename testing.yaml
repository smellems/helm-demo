environments:
  default:
    values:
      - baseDomain: 

---

helmDefaults:
  wait: false

repositories:
  - name: bitnami
    url: https://charts.bitnami.com/bitnami

releases:
  - name: apache
    chart: bitnami/apache
    version: 11.3.5
    namespace: seb
    values:
      - ingress:
          enabled: true
          hostname: apache.{{ .Values.baseDomain }}
          ingressClassName: nginx
          path: /
      - pdb:
          create: true
    labels:
      app: apache

